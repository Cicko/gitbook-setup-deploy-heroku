// There the heroku app should be created.
// There the oauth app should be created.
// There the remote url for the app have to be added to git.


module.exports.install = (callback) => {
  callback(null, "install del plugin");
}
